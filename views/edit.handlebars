</form>
<form method="POST" action="/todo/{{{todo.id}}}/edit?_method=PUT">
  <div class="input-group md-3">
    <div class="input-group-prepend">
      <div class="input-group-text">
        <input type="checkbox" name="done" {{#if todo.done}}checked{{/if}}>

      </div>
    </div>
    <input type="text" placeholder="name" name="name" value="{{{todo.name}}}" class="form-control">
    <div class="input-group-append">
      <button type="submit" class='input-group-text btn btn-success'>Edit</button>
    </div>
  </div>

</form>